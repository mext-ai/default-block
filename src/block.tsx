import React from 'react';

// Define the props interface for this block
// This interface will be automatically parsed by the MEXT system
// when you push files to GitHub via Sandpack editor
interface BlockProps {
  
}



const Block: React.FC<BlockProps> = (props) => {
  
  return (
    <div>
      {/* start coding here */}
    </div>
  );
};

export default Block;
